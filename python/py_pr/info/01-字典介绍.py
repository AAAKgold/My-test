#字典和列表一样，也能够存储多个数据

#列表中找某个元素时，是根据下标进行的
#字典中找某个元素时，是根据‘名字’（就是冒号前面的那个值）

#字典的每个元素由两部分组成  键:值  

info = {'name':'班长','id':'100','address':'地球亚洲大陆'}

print(info['name'])

#print(info['address'])#若访问不存在的键 则会报错

#age = info.get('age')
#print(age)#age 键不存在 所以None

#age = info.get('age',18)
#print(age)#age 键存在 所以返回默认


















